for this project "Valtrust" you need to make sure this.

IMPORTANT: 

You should have the latest version of Node.js this means Node.js 26.0.0 or 
Then you should proceed to install a ReactBits style "", this will prevent you from having problems while "npm run dev"

for backend you should install "pip install flask-cors", this will allow python to mix with js and show results on HTML

Also you will have to install pip install...